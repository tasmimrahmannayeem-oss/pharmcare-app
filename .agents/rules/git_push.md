---
description: Ensure all commits are pushed to the remote GitHub repository
---

# Git Commit and Push Rule

Whenever you are asked to commit changes to the repository, you **MUST** also push those changes to the remote repository (e.g., `git push origin main` or `git push`) immediately after committing. 

Do not leave commits sitting locally unless explicitly asked to do so by the user.
